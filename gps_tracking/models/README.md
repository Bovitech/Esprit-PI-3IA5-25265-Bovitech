# GPS LSTM weights

Not stored in Git. Downloaded from [Amiiimi/GPS](https://huggingface.co/Amiiimi/GPS) via `models.manifest.json`:

- `lstm_all.pth`
- `scaler_all.pkl`

From repo root:

```bash
python scripts/download_models.py
```

Required for `/gps/dashboard/` on the ML API.
