# Bovitech — install from repo root (Python 3.10+ and Node.js 18+):
#
#   pip install -r requirements.txt
#   cd main-bovitech-main && npm install && cd ..
#
# pip → this file (ML API, PI_Backend, …)
# npm → main-bovitech-main/package.json (Expo app)

# --- ML inference API (src/model_http_api.py) ---
pandas>=2.0.0,<3
numpy>=1.24.0,<3
scikit-learn>=1.3.0,<2
xgboost>=2.0.0,<3
joblib>=1.3.0,<2
python-dotenv>=1.0.0,<2
torch>=2.1.0,<3
tensorflow>=2.15.0,<3
librosa>=0.10.0,<1
soundfile>=0.12.0,<1
scipy>=1.10.0,<2
stable-baselines3>=2.0.0,<3
gymnasium>=0.29.0,<2
pyserial>=3.5,<4

# --- Optional dashboards / tooling ---
streamlit>=1.28.0,<2
plotly>=5.18.0,<7

# --- PI_Backend (Django login / herd API) ---
Django>=5.2,<6
djangorestframework>=3.15,<4
djangorestframework-simplejwt>=5.4,<6
django-cors-headers>=4.6,<5

# Optional: illness SHAP explanations — pip install shap
