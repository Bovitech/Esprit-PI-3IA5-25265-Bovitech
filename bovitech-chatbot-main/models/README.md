# Skin disease model weights

Not stored in Git. Downloaded from [Amiiimi/DiseaseClassification](https://huggingface.co/Amiiimi/DiseaseClassification) via `models.manifest.json`:

- `best_model.pth`

From repo root:

```bash
python scripts/download_models.py
```

Required for chatbot skin agent (`/chatbot/skin/`).
